import { createStore } from 'vuex'
import {blogStore} from "@/store/blogStore";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    blogStore
  }
})
